'use strict'

let menuSlide = () => {

    /*    opens and closes menu */
    let menu = document.getElementById("menuClose")
    menu.classList.toggle("menuOpen")
    menu.style.display = 'block'
    menu.style.width = '250px'
}